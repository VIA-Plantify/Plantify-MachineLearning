name: dev
services:
  maldev:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    container_name: maldev
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: sleep infinity
    environment:
      GRPC_HOST: host.docker.internal
      GRPC_PORT: 10081
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "12080:12080"

  maldev-gpu:
    build:
      context: .
      dockerfile: .devcontainer-gpu/.devcontainer/Dockerfile
    container_name: maldev-gpu
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: sleep infinity
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      GRPC_HOST: host.docker.internal
      GRPC_PORT: 10081
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "12082:12080"